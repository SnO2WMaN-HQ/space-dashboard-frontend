query IsSpaceFollowing($spaceId: ID!) {
  currentUser {
    isFollowingSpace(spaceId: $spaceId)
  }
}

mutation FollowSpace($spaceId: ID!, $userId: ID!) {
  followSpace(spaceId: $spaceId, userId: $userId) {
    user {
      id
    }
    space {
      id
    }
  }
}
