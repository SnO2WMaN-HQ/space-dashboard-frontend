mutation Register($uniqueName: ID!, $displayName: String!, $picture: String!) {
  createUser(
    uniqueName: $uniqueName
    displayName: $displayName
    picture: $picture
  ) {
    id
    uniqueName
    displayName
    picture
  }
}
